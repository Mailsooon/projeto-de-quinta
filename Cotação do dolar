<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cotações Dólar e Euro</title>
    <script crossorigin src="https://unpkg.com/react@18/umd/react.development.js"></script>
    <script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: #333;
        }
        .container {
            max-width: 800px;
            width: 100%;
            padding: 20px;
            text-align: center;
            background-color: rgba(255, 255, 255, 0.9);
            border-radius: 15px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
            backdrop-filter: blur(10px);
        }
        h1 {
            color: #4a4a4a;
            margin-bottom: 30px;
            font-size: 2.5em;
        }
        .currency {
            display: inline-block;
            width: calc(50% - 20px);
            margin: 10px;
            padding: 25px;
            border: 1px solid #ddd;
            border-radius: 12px;
            background-color: #fff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s, box-shadow 0.2s;
        }
        .currency:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }
        .currency h2 {
            margin-top: 0;
            color: #2c3e50;
            font-size: 1.5em;
        }
        .currency p {
            margin: 10px 0;
            font-size: 1.1em;
        }
        .loading {
            color: #7f8c8d;
            font-style: italic;
        }
        .error {
            color: #e74c3c;
            font-weight: bold;
            margin: 20px 0;
        }
        @media (max-width: 600px) {
            .currency {
                width: calc(100% - 20px);
            }
        }
    </style>
</head>
<body>
    <div id="root"></div>

    <script type="text/babel">
        const { useState, useEffect } = React;

        function CurrencyComponent({ currencyCode, data }) {
            if (!data) {
                return <div className="currency loading">Carregando...</div>;
            }

            const { bid, ask, high, low } = data;
            return (
                <div className="currency">
                    <h2>{currencyCode === 'USDBRL' ? 'Dólar (USD)' : 'Euro (EUR)'} em Real (BRL)</h2>
                    <p><strong>Compra:</strong> R$ {parseFloat(bid).toFixed(4)}</p>
                    <p><strong>Venda:</strong> R$ {parseFloat(ask).toFixed(4)}</p>
                    <p><strong>Máxima:</strong> R$ {parseFloat(high).toFixed(4)}</p>
                    <p><strong>Mínima:</strong> R$ {parseFloat(low).toFixed(4)}</p>
                </div>
            );
        }

        function App() {
            const [usdData, setUsdData] = useState(null);
            const [eurData, setEurData] = useState(null);
            const [error, setError] = useState(null);

            const fetchData = async () => {
                try {
                    const response = await fetch('https://economia.awesomeapi.com.br/json/last/USD-BRL,EUR-BRL');
                    if (!response.ok) {
                        throw new Error('Erro ao buscar dados');
                    }
                    const data = await response.json();
                    setUsdData(data.USDBRL);
                    setEurData(data.EURBRL);
                    setError(null);
                } catch (err) {
                    setError(err.message);
                }
            };

            useEffect(() => {
                fetchData();
                const interval = setInterval(fetchData, 30000); // Atualiza a cada 30 segundos
                return () => clearInterval(interval);
            }, []);

            return (
                <div className="container">
                    <h1>Cotações Atuais</h1>
                    {error && <p className="error">Erro: {error}</p>}
                    <CurrencyComponent currencyCode="USDBRL" data={usdData} />
                    <CurrencyComponent currencyCode="EURBRL" data={eurData} />
                </div>
            );
        }

        ReactDOM.render(<App />, document.getElementById('root'));
    </script>
</body>
</html>
